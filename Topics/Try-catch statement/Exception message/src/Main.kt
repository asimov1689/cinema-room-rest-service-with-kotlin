// You can experiment here, it won't be checked

fun main() {
    try {
        problemFunction()
    } catch (e: Exception) {
        println(e.message)
    }
}

private fun problemFunction() {
    // Placeholder implementation to keep this sandbox file compilable.
}
